<!DOCTYPE html>
<html lang="en">

<head>
    <link rel="stylesheet" href="/css/style.css" />
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Vortex Guild Home Page</title>
</head>

<body>
<header>

    <div class="columns">
        <img src="/images/vortex.png" alt="">

        <nav class="menu">
            <ul>
                <li><a href="/" class="<%= navLocation == 'main' ? 'active' : ''%>">Home page</a></li>
                <li>
                    <% if(loggedUser) { %>
                    <a href="/players" class="<%= navLocation == 'players' ? 'active' : ''%>">Players</a></li>
                    <% } %>
                <li>
                    <% if(loggedUser) { %>
                    <a href="/raids" class="<%=navLocation == 'raids' ? 'active' : ''%>">Raids</a>
                    <% } %>
                </li>
                <li>
                    <% if(loggedUser) { %>
                    <a href="/signups" class="<%=navLocation == 'signups' ? 'active' : ''%>">Signups</a>
                    <% } %>
                </li>
                <li><a href="/login" class="<%=navLocation == 'login' ? 'active' : ''%>">Login</a></li>
            </ul>

        </nav>
        <% if(loggedUser) { %>
            <h2>Welcome <%= loggedUser.ingameName%></h2>
        <% } %>



    </div>

</header>